# Inference-only requirements for the SurgMotion + v5 ensemble submission image.
# Union of requirements.surgmotion.txt (SurgMotion branch) and requirements.txt
# (v5/Qwen2-VL-2B branch, needs qwen-vl-utils for predict.py's process_vision_info).
# torch/torchvision come from the pytorch/pytorch base image and are not reinstalled.
transformers==4.57.6
peft==0.17.1
timm==1.0.28
accelerate
einops
decord
opencv-python
numpy
nltk
sentencepiece
safetensors
tqdm
qwen-vl-utils
